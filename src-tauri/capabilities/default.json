{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the local desktop-pet window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
